<div class="form-group flexrow">
    <input class="data-path-input" type="text" name="{{this.module}}.{{this.key}}.filters.{{index}}.filterpath" placeholder="{{localize 'path'}}">
        <select name="{{this.module}}.{{this.key}}.filters.{{index}}.comparison">
            <option value="=="><span title="equals">==</span></option>
            <option value="<="><span title="smaller or equal">&lt;=</span></option>
            <option value=">="><span title="bigger or equal">&gt;=</span></option>
            <option value="!="><span title="is not">!=</span></option>
        </select>
    <input type="text" name="{{this.module}}.{{this.key}}.filters.{{index}}.value">
    <div class="actions">
        <button data-action="deleteRow"><i class="fas fa-trash" data-action="deleteRow"></i></button>
    </div>
</div>

